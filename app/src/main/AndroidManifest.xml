<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="jain.pranjal.dailyhisab">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_1"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_1"
        android:supportsRtl="true">
        <activity android:name=".FrontPage" android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" android:theme="@style/style1"/>
        <activity android:name=".Insert" android:theme="@style/style1"/>
        <activity android:name=".Edit" android:theme="@style/style1" />
        <activity android:name=".CalanderActivity" android:theme="@style/style1"/>
    </application>

</manifest>